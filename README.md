# منصة نخبة 🎓

منصة تعليمية متكاملة لمشاركة الملفات والمعرفة بين الطلاب والطالبات

## المميزات الرئيسية ✨

- نظام تسجيل دخول آمن باستخدام البريد الإلكتروني وكلمة المرور
- رفع ومشاركة الملفات الدراسية
- تصنيف الملفات حسب الفئات (محاضرات، واجبات، مشاريع، كتب، ملخصات)
- البحث عن الملفات حسب العنوان والوصف والتصنيف
- عرض إحصائيات المشاهدات والتحميلات
- واجهة مستخدم عصرية وسهلة الاستخدام
- تصميم متجاوب يعمل على جميع الأجهزة

## التقنيات المستخدمة 🛠

### Backend (الخادم الخلفي)
- **Node.js** - بيئة تشغيل JavaScript
- **Express.js** - إطار عمل للخادم
- **MongoDB** - قاعدة البيانات
- **Mongoose** - ORM لقاعدة البيانات
- **JWT** - المصادقة والأمان
- **Multer** - رفع الملفات
- **bcryptjs** - تشفير كلمات المرور

### Frontend (الواجهة الأمامية)
- **React 18** - مكتبة بناء الواجهات
- **React Router** - التنقل بين الصفحات
- **Axios** - التواصل مع API
- **Context API** - إدارة الحالة

## متطلبات التشغيل 📋

- Node.js (الإصدار 14 أو أحدث)
- MongoDB (الإصدار 4 أو أحدث)
- npm أو yarn

## التثبيت والتشغيل 🚀

### الطريقة السريعة (باستخدام ملف cf) ⚡

```bash
# منح صلاحيات التنفيذ
chmod +x cf

# تثبيت الحزم
./cf install

# تشغيل المشروع
./cf start

# عرض المساعدة
./cf help
```

**للمزيد من التفاصيل:** راجع [CF_GUIDE.md](CF_GUIDE.md)

---

### الطريقة التقليدية

### 1. استنساخ المشروع
```bash
git clone <repository-url>
cd nokhba
```

### 2. إعداد الخادم الخلفي (Backend)

```bash
cd backend

# تثبيت المكتبات
npm install

# إنشاء ملف .env
cp .env.example .env

# تعديل ملف .env وإضافة الإعدادات الخاصة بك:
# MONGODB_URI=mongodb://localhost:27017/nokhba
# JWT_SECRET=your_secret_key_here
# PORT=5000

# تشغيل الخادم
npm start

# أو للتطوير مع التحديث التلقائي
npm run dev
```

الخادم سيعمل على: `http://localhost:5000`

### 3. إعداد الواجهة الأمامية (Frontend)

```bash
cd frontend

# تثبيت المكتبات
npm install

# تشغيل التطبيق
npm start
```

التطبيق سيفتح تلقائياً على: `http://localhost:3000`

## الهيكل العام للمشروع 📁

```
nokhba/
├── backend/
│   ├── models/          # نماذج قاعدة البيانات
│   │   ├── User.js
│   │   └── File.js
│   ├── routes/          # مسارات API
│   │   ├── auth.js
│   │   ├── files.js
│   │   └── users.js
│   ├── middleware/      # الوسيطات
│   │   └── auth.js
│   ├── uploads/         # الملفات المرفوعة
│   ├── server.js        # الملف الرئيسي
│   ├── package.json
│   └── .env.example
│
└── frontend/
    ├── public/
    │   └── index.html
    ├── src/
    │   ├── components/  # المكونات
    │   │   └── Navbar.js
    │   ├── context/     # Context API
    │   │   └── AuthContext.js
    │   ├── pages/       # الصفحات
    │   │   ├── Home.js
    │   │   ├── Login.js
    │   │   ├── Register.js
    │   │   └── Files.js
    │   ├── services/    # خدمات API
    │   │   └── fileService.js
    │   ├── App.js
    │   └── index.js
    └── package.json
```

## API Endpoints 🔌

### المصادقة (Authentication)
- `POST /api/auth/register` - تسجيل مستخدم جديد
- `POST /api/auth/login` - تسجيل الدخول
- `GET /api/auth/me` - الحصول على بيانات المستخدم الحالي

### الملفات (Files)
- `GET /api/files` - الحصول على جميع الملفات
- `GET /api/files/:id` - الحصول على ملف محدد
- `POST /api/files/upload` - رفع ملف جديد (يتطلب تسجيل دخول)
- `GET /api/files/download/:id` - تحميل ملف
- `DELETE /api/files/:id` - حذف ملف (يتطلب تسجيل دخول)
- `GET /api/files/user/my-files` - الحصول على ملفات المستخدم

### المستخدمين (Users)
- `GET /api/users` - الحصول على جميع المستخدمين (Admin فقط)
- `GET /api/users/:id` - الحصول على مستخدم محدد
- `PUT /api/users/profile` - تحديث الملف الشخصي

## الاستخدام 📖

### 1. إنشاء حساب جديد
- انتقل إلى صفحة التسجيل
- أدخل البيانات المطلوبة (الاسم، البريد الإلكتروني، كلمة المرور)
- يمكنك إضافة معلومات اختيارية (الرقم الجامعي، التخصص، الجامعة)

### 2. تسجيل الدخول
- استخدم البريد الإلكتروني وكلمة المرور للدخول

### 3. رفع ملف
- من صفحة الملفات، اضغط على "رفع ملف جديد"
- اختر الملف وأدخل المعلومات المطلوبة
- حدد التصنيف المناسب

### 4. تحميل ملف
- ابحث عن الملف المطلوب
- اضغط على زر "تحميل"

### 5. البحث عن ملفات
- استخدم شريط البحث للبحث حسب العنوان أو الوصف
- استخدم فلتر التصنيفات لتصفية النتائج

## الأمان 🔒

- تشفير كلمات المرور باستخدام bcryptjs
- المصادقة باستخدام JWT tokens
- حماية المسارات الخاصة
- التحقق من صحة البيانات المدخلة
- حماية من SQL Injection و XSS

## التطوير المستقبلي 🚧

- إضافة نظام التعليقات على الملفات
- نظام التقييمات والمراجعات
- إشعارات البريد الإلكتروني
- دردشة فورية بين المستخدمين
- نظام الإشعارات الفورية
- تحسينات في محرك البحث
- إمكانية إضافة الملفات المفضلة
- إحصائيات متقدمة

## المساهمة 🤝

نرحب بالمساهمات! لا تتردد في:
- فتح Issue للإبلاغ عن مشكلة
- إنشاء Pull Request لإضافة ميزة جديدة
- اقتراح تحسينات

## الترخيص 📄

MIT License

## التواصل 📧

لأي استفسارات أو اقتراحات، يرجى التواصل معنا.

---

تم التطوير بواسطة فريق منصة نخبة 💙
