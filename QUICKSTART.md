# دليل البدء السريع - منصة نخبة 🚀

## الخطوات السريعة للتشغيل

### 1️⃣ تثبيت MongoDB
تأكد من تثبيت MongoDB على جهازك وتشغيله:

```bash
# macOS (باستخدام Homebrew)
brew install mongodb-community
brew services start mongodb-community

# أو تشغيل MongoDB مباشرة
mongod --dbpath=/path/to/data
```

### 2️⃣ تشغيل الخادم الخلفي (Backend)

افتح نافذة Terminal وقم بتنفيذ:

```bash
cd backend
npm install
npm start
```

يجب أن ترى:
```
✓ تم الاتصال بقاعدة البيانات بنجاح
🚀 الخادم يعمل على المنفذ 5000
📍 الرابط: http://localhost:5000
```

### 3️⃣ تشغيل الواجهة الأمامية (Frontend)

افتح نافذة Terminal جديدة:

```bash
cd frontend
npm install
npm start
```

سيتم فتح المتصفح تلقائياً على `http://localhost:3000`

## اختبار المنصة ✅

### إنشاء حساب تجريبي:
1. اذهب إلى صفحة التسجيل
2. أدخل البيانات:
   - الاسم: محمد أحمد
   - البريد: test@nokhba.com
   - كلمة المرور: 123456
   - الرقم الجامعي: 123456
   - التخصص: علوم الحاسب
   - الجامعة: جامعة الملك سعود

### رفع ملف تجريبي:
1. سجل الدخول
2. اذهب إلى صفحة "الملفات"
3. اضغط على "رفع ملف جديد"
4. اختر ملف PDF أو Word
5. أدخل عنوان ووصف الملف
6. اختر التصنيف المناسب
7. اضغط "رفع الملف"

## المنافذ المستخدمة 🔌

- **Backend API**: `http://localhost:5000`
- **Frontend**: `http://localhost:3000`
- **MongoDB**: `mongodb://localhost:27017`

## حل المشاكل الشائعة 🔧

### المشكلة: خطأ في الاتصال بقاعدة البيانات
**الحل**: تأكد من تشغيل MongoDB:
```bash
brew services start mongodb-community
```

### المشكلة: Port 5000 مستخدم
**الحل**: غير المنفذ في ملف `.env`:
```
PORT=5001
```

### المشكلة: خطأ CORS
**الحل**: تأكد من أن `proxy` محدد في `frontend/package.json`:
```json
"proxy": "http://localhost:5000"
```

### المشكلة: لا يتم رفع الملفات
**الحل**: تأكد من وجود مجلد uploads:
```bash
cd backend
mkdir uploads
```

## الحسابات الافتراضية 👤

لا توجد حسابات افتراضية، يجب إنشاء حساب جديد من صفحة التسجيل.

## الخطوات التالية 📚

- اقرأ ملف [README.md](README.md) للمزيد من التفاصيل
- جرب رفع وتحميل الملفات
- استكشف ميزات البحث والتصفية
- جرب تحديث الملف الشخصي

## الدعم 💬

إذا واجهت أي مشكلة:
1. تحقق من تشغيل MongoDB والخادم الخلفي
2. تحقق من console في المتصفح للأخطاء
3. تحقق من logs الخادم في Terminal

---

استمتع باستخدام منصة نخبة! 🎓✨
