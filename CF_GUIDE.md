# دليل استخدام ملف cf ⚡

ملف `cf` (Command File) هو أداة سريعة لإدارة مشروع منصة نخبة بسهولة.

## التثبيت الأولي 📦

```bash
# امنح الملف صلاحيات التنفيذ (مرة واحدة فقط)
chmod +x cf
```

## الأوامر المتاحة 🎯

### 1. تشغيل المشروع كاملاً
```bash
./cf start
```
يشغل Backend و Frontend معاً

### 2. تشغيل Backend فقط
```bash
./cf backend
```
يشغل الخادم الخلفي على المنفذ 5001

### 3. تشغيل Frontend فقط
```bash
./cf frontend
```
يشغل واجهة React على المنفذ 3000

### 4. إيقاف جميع الخوادم
```bash
./cf stop
```
يوقف Backend و Frontend

### 5. تثبيت الحزم
```bash
./cf install
```
يثبت حزم npm لـ Backend و Frontend

### 6. تشغيل MongoDB
```bash
./cf mongodb
```
يشغل قاعدة بيانات MongoDB

### 7. عرض حالة الخوادم
```bash
./cf status
```
يعرض حالة Backend و Frontend و MongoDB

### 8. تنظيف الملفات المؤقتة
```bash
./cf clean
```
يحذف node_modules والملفات المؤقتة

### 9. عرض المساعدة
```bash
./cf help
```
يعرض جميع الأوامر المتاحة

### 10. القائمة التفاعلية
```bash
./cf
```
يعرض قائمة تفاعلية للاختيار من بينها

## أمثلة الاستخدام 💡

### البدء السريع
```bash
# تثبيت الحزم (المرة الأولى فقط)
./cf install

# تشغيل MongoDB (اختياري)
./cf mongodb

# تشغيل المشروع
./cf start
```

### التطوير اليومي
```bash
# فحص الحالة
./cf status

# تشغيل المشروع
./cf start

# عند الانتهاء
./cf stop
```

### التنظيف وإعادة التثبيت
```bash
# تنظيف الملفات
./cf clean

# إعادة تثبيت الحزم
./cf install

# تشغيل المشروع
./cf start
```

## الروابط السريعة 🔗

بعد التشغيل، يمكنك الوصول إلى:

- **الصفحة الرئيسية:** `http://localhost:3000`
- **مبادرة التفوق المستمر:** `http://localhost:3000/excellence`
- **الدورات:** `http://localhost:3000/excellence/courses`
- **البحث العلمي:** `http://localhost:3000/excellence/research`
- **الدروس الإثرائية:** `http://localhost:3000/excellence/enrichment`
- **الاستفسارات:** `http://localhost:3000/excellence/inquiries`
- **API Backend:** `http://localhost:5001`

## الإيقاف ⏹️

### إيقاف تلقائي
```bash
./cf stop
```

### إيقاف يدوي
اضغط `Ctrl+C` في Terminal حيث يعمل الخادم

## استكشاف الأخطاء 🔧

### المنفذ مستخدم
```bash
# أوقف جميع الخوادم أولاً
./cf stop

# ثم شغل من جديد
./cf start
```

### مشاكل في الحزم
```bash
# نظف وأعد التثبيت
./cf clean
./cf install
```

### MongoDB لا يعمل
```bash
# تأكد من تثبيت MongoDB
brew install mongodb-community

# شغل MongoDB
./cf mongodb
```

## المميزات ✨

- ✅ تشغيل سريع بأمر واحد
- ✅ ألوان واضحة للرسائل
- ✅ إدارة العمليات تلقائياً
- ✅ فحص الحالة السريع
- ✅ تنظيف سهل
- ✅ دعم كامل للعربية

## ملاحظات 📝

- الملف يعمل على macOS و Linux
- يحتاج Node.js و npm مثبتين
- MongoDB اختياري للتطوير
- يمكن تشغيل Backend و Frontend منفصلين

---

**تم إنشاء الملف بواسطة:** منصة نخبة 🎓
**التاريخ:** ديسمبر 2024
